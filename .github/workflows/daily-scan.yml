name: Daily Signal Scan (retired)

# The FastAPI lifespan scheduler is the sole supported auto-scan authority.
# Keep this manual workflow as a visible retirement switch rather than running
# a second broadcaster alongside the Render worker.
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: echo "Auto-scan is handled by the single-worker FastAPI scheduler; no external delivery is performed here."
